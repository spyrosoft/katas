QUnit.test( "Pythagorean Triplet Exists", function( assert ) {
	assert.ok( triplet, "Triplet Exists!!" );
});

QUnit.test( "A + B + C = 1000", function( assert ) {
	assert.ok( a + b + c == 1000 );
});
